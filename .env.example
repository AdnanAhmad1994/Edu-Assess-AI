# ============================================================
#  EduAssess AI — Environment Variables
#  Copy this file to .env for local development.
#  For production (Vercel), add these in:
#  Vercel Dashboard → Project → Settings → Environment Variables
# ============================================================

# ── Database (Required for persistent sessions & data) ──────
# Get a free PostgreSQL database at: https://neon.tech
DATABASE_URL=postgresql://user:password@host/dbname

# ── Session Security (Required) ──────────────────────────────
# Generate a strong random string: openssl rand -hex 32
SESSION_SECRET=change-this-to-a-long-random-secret-string

# ── AI Provider Keys (Add at least ONE) ──────────────────────
# The first non-empty key wins (priority: OpenRouter > Gemini > OpenAI)
# These auto-configure the admin account on first startup.

# OpenRouter (access to 100+ models): https://openrouter.ai/keys
OPENROUTER_API_KEY=

# Google Gemini: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=
AI_INTEGRATIONS_GEMINI_API_KEY=

# OpenAI: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Grok (xAI): https://console.x.ai
GROK_API_KEY=

# Kimi: https://platform.moonshot.cn
KIMI_API_KEY=

# Anthropic Claude: https://console.anthropic.com/keys
ANTHROPIC_API_KEY=

# Custom OpenAI-compatible provider
CUSTOM_API_KEY=
CUSTOM_BASE_URL=
CUSTOM_MODEL=

# ── Email (Optional — for password reset) ────────────────────
# Get from: https://resend.com
RESEND_API_KEY=

# ── Object Storage (Optional — for file uploads) ─────────────
# Google Cloud Storage bucket name
GCS_BUCKET_NAME=

# ── App URL (Required in production) ─────────────────────────
# Your Vercel deployment URL
APP_URL=https://your-app.vercel.app
